# --- Alap UI / app ---
streamlit>=1.36
streamlit-option-menu>=0.3.12

# --- Adatkezelés / DB réteg ---
pandas>=2.2
sqlalchemy>=2.0

# --- PostgreSQL driver ---
psycopg2-binary>=2.9

# --- HTTP hívások (VIR integráció) ---
requests>=2.31

# --- PDF export + képek ---
reportlab>=4.0
pillow>=10.0
# --- Mérleghez ---
pyserial>=3.5

# --- (OPCIONÁLIS) .env támogatás lokálra ---
# python-dotenv>=1.0

# --- (OPCIONÁLIS) MySQL/MariaDB driver, ha valaha kellene ---
# pymysql>=1.1

# --- (OPCIONÁLIS) Keyring tárolás lokális gépen
# FIGYELEM: Streamlit Cloudon tipikusan nincs natív backend.
# Ha mindenképp használni szeretnéd, próbáld:
# keyring>=25.2
# keyrings.alt>=5.0
#
# macOS esetén (helyi fejlesztésnél) a beépített Keychain működik keyring-gel.
# Linux szerveren célszerűbb a jelszót Streamlit Secrets-be vagy env változóba tenni.
